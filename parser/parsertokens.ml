open Tigerparse
type linenum = int
type token = Tigerparse.token
let _TYPE (i,j) = TYPE
let _VAR (i,j) = VAR
let _FUNCTION (i,j) = FUNCTION
let _BREAK (i,j) = BREAK
let _OF (i,j) = OF
let _END (i,j) = END
let _IN (i,j) = IN
let _NIL (i,j) = NIL
let _LET (i,j) = LET
let _DO (i,j) = DO
let _TO (i,j) = TO
let _FOR (i,j) = FOR
let _WHILE (i,j) = WHILE
let _ELSE (i,j) = ELSE
let _THEN (i,j) = THEN
let _IF (i,j) = IF
let _ARRAY (i,j) = ARRAY 
let _ASSIGN (i,j) = ASSIGN
let _OR (i,j) = OR
let _AND (i,j) = AND
let _GE (i,j) = GE
let _GT (i,j) = GT
let _LE (i,j) = LE
let _LT (i,j) = LT
let _NEQ (i,j) = NEQ
let _EQ (i,j) = EQ
let _DIV (i,j) = DIV
let _MUL (i,j) = MUL
let _MINUS (i,j) = MINUS
let _PLUS (i,j) = PLUS
let _DOT (i,j) = DOT
let _RBRACE (i,j) = RBRACE
let _LBRACE (i,j) = LBRACE
let _RBRACK (i,j) = RBRACK
let _LBRACK (i,j) = LBRACK
let _RPAREN (i,j) = RPAREN
let _LPAREN (i,j) = LPAREN
let _SEMICOLON (i,j) = SEMICOLON
let _COLON (i,j) = COLON
let _COMMA (i,j) = COMMA
let _STRING (s,i,j) = STRING(s)
let _INT (c,i,j) = INT(c)
let _ID (s,i,j) = ID(s)
let _EOF (i,j) = EOF
let _COMMENT (i,j) = () 
